/**
 * Map configuration for the 'test-metro' theme.
 */

import type { MapConfig } from '../types';

export const mapConfig: MapConfig = {
  backgroundImage: '', // No background image for test
  locationPositions: [
    {
      locationId: 'metro',
      x: 50,
      y: 50,
      size: 'lg',
    },
  ],
};
